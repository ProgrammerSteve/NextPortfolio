import "./globals.css";
import { Inter } from "next/font/google";
import { Analytics } from "@vercel/analytics/react";

const inter = Inter({ subsets: ["latin"] });

export const metadata = {
  title: "Steven Bautista Portfolio Site",
  description:
    "Steven Bautista, React Fullstack developer familiar with MERN stack. Some technologies used: Typescript, Node.js, Next.js, Express.js, React, Python",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Analytics />
      </body>
    </html>
  );
}
